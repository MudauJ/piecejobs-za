Update the Super Admin dashboard to match all current homeowner and worker features.

## 1. JOBS TABLE UPDATES
In the admin Jobs tab, update the jobs table to show:
- All categories as badges (not just single category) — use the categories array field
- Bundle badge 📦 for booking_type = 'bundle'
- Multi-worker badge 👥 for booking_type = 'multi'
- Scheduled date and time columns
- When admin clicks on a job, the full detail modal shows all categories, booking type, scheduled date/time, and all messages from the Secure Job Chat for that job

## 2. WORKERS TABLE UPDATES
In the admin Workers tab, update the worker full profile modal to show:
- Badge level with emoji (🥉🥈🥇💎) and completed jobs count
- Referral code
- Number of successful referrals
- Referral earnings (R amount)
- Notification count (pending notifications in notifications_queue)

## 3. NEW MESSAGES TAB
Add a "Messages 💬" tab to the admin sidebar showing:
- All Secure Job Chat conversations grouped by job
- Each conversation shows: job title, homeowner name, worker name, last message, date
- Click on a conversation to see the full chat thread
- Admin can see all messages but cannot send messages (read-only)
- Show total message count badge on the tab

## 4. NEW NOTIFICATIONS TAB  
Add a "Notifications 🔔" tab to the admin sidebar showing:
- All records from notifications_queue
- Columns: Worker name, Job title, Message, Status (pending/read), Date
- Filter by status (pending/read)
- "Mark all as read" button
- Count badge showing pending notifications

## 5. EMAIL QUEUE TAB
Add an "Emails 📧" tab to the admin sidebar showing:
- All records from email_notifications table
- Columns: To email, Subject, Status, Date
- Filter by status (pending/sent/failed)
- "Resend" button on failed emails
- Count badge showing pending emails

## 6. ADMIN OVERVIEW UPDATES
Add these new stats to the admin overview:
- Total messages sent (count from messages table)
- Pending notifications (count from notifications_queue where status = 'pending')
- Bundle bookings vs Single bookings (count from jobs grouped by booking_type)
- Most popular category (from categories array across all jobs)

Use direct fetch to Supabase REST API with hardcoded credentials:
URL: https://vnrvwfialfvduvetoewa.supabase.co
KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZucnZ3ZmlhbGZ2ZHV2ZXRvZXdhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI3NTUzMjYsImV4cCI6MjA5ODMzMTMyNn0.5mfElVG_tuhBLLP4BKdQ7v5zXLIi51LpMbZUmKZ8A9w

Keep the existing navy/blue/amber design system.
Push to GitHub MudauJ/piecejobs-za on main branch and republish on Replit when done.